apiVersion: v1
kind: Service

metadata:
  name: video-api
  labels:
    name: video-api
    tier: backend

spec:
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    name: video-api
    tier: backend
